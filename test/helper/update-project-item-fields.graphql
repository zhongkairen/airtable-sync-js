mutation UpdateProjectItemField(
  $projectId: ID!
  $itemId: ID!
  $fields: [ProjectNextItemFieldValueInput!]!
) {
  updateProjectNextItemFieldValues(
    input: { projectId: $projectId, itemId: $itemId, fieldValues: $fields }
  ) {
    projectNextItem {
      id
    }
  }
}
